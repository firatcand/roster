# Roster host activation fixture

When the user's entire request is `ROSTER_ACTIVATION_FIXTURE`, reply with exactly `ROSTER_CLAUDE_RULE_LOADED` and nothing else.

When the user's entire request is `ROSTER_LIFECYCLE_POINTER_FIXTURE`, read `ROSTER.md` and follow its fixture instruction exactly.
