# Global ops agents

Logic for ops agents. No agents yet — add via:

```bash
bash scripts/new-agent.sh ops <agent-name>
```

When you add the first agent here, see `gtm/sdr/` as the canonical example.
