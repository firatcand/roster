# Issue #343 implementation plan — ratify the host-owned runtime contract

## Ticket

- GitHub: `firatcand/roster#343`
- Forge task: `P1-T01`
- Branch: `feat/343-ratify-host-owned-runtime-contract`
- Type: documentation and repository lifecycle bootstrap

## Outcome

Make the already-approved Roster v2 architecture durable and clone-portable. Git owns product policy and planning artifacts, GitHub owns live execution state, and the npm package continues to exclude internal product artifacts through its explicit `files` allowlist.

## Questions asked and answers applied

1. **Public product-authority lifecycle** (`file_lifecycle`, project blast radius)
   - Asked by Codex after the initial repository audit and three Claude Opus plan reviews.
   - Human answer on 2026-08-01: **Yes, publish.**
   - Applied branch: Git-track the canonical Roster v2 specification, roadmap snapshot, durable investigation, decision index, and approved task plans, including the named `my-roster` and `roster-lobu` proof scenarios.
   - Rejected branch: ignored local-only authority, because it breaks fresh-clone bootstrap, `/update-spec` commits, committed `/plan-task` plans, and durable decision history.

No other architectural fork remains. Routine formatting and stale-file classification follow the installed Forge 0.4.5 contracts and the approved public lifecycle.

## Predicted tracked files

- `.gitignore`
- `AGENTS.md`
- `CLAUDE.md`
- `spec/BRIEF.md`
- `spec/PRD.md`
- `spec/SPEC.md`
- `spec/CONTEXT.md`
- `spec/decisions/INDEX.md`
- `plans/phases.yaml`
- `plans/tasks/ROSTER-V2-RUNTIME-ACTIVATION.investigation.md`
- `plans/tasks/343.plan.md`

No runtime source, package dependency, shipped template, or `.forge/settings.yaml` change belongs in this ticket.

## Implementation sequence

### 1. Bootstrap canonical Git authority

- Remove only the root `/spec/` and `/plans/` ignore rules.
- Keep `.forge` runtime state, worktrees, Loom data, dependencies, and build output ignored.
- Track only the canonical Roster v2 artifacts listed above.
- Move the hydrated legacy `spec/SPEC-ROS-140.md` to `.forge/drafts/legacy/SPEC-ROS-140.md` before removing the ignore rules. This preserves a recoverable local copy under the existing `/.forge/*` ignore while leaving no untracked residue in the canonical tree.
- During post-merge local wrap-up—not while implementing in the task worktree—move the main checkout's stale `spec/decisions/2026-06-23-roster-brain.md` to `.forge/drafts/legacy/2026-06-23-roster-brain.md` as approved local-only legacy material. It must not remain in the active decision directory, where `/update-spec --draft` would reject every future decision.
- Do not track either stale file. The superseded Brain decision is represented by an unlinked legacy entry in the normalized decision index, the current SPEC, and already-tracked historical v1 design documents.

### 2. Normalize Forge lifecycle documentation

- Rewrite `spec/decisions/INDEX.md` to the installed apply engine's canonical `# Decision index` plus bullet-row format.
- Record the legacy `roster-brain` decision as superseded by the v2 contract without a dangling link to an active ADR; cite the tracked historical successors in `docs/superpowers/specs/2026-06-23-roster-brain-design.md` and `docs/adr/0003-brain-file-system.md`.
- Keep the bullet list as the final structure in the file so Forge's append-only index writer extends it correctly. The literal `` `roster-brain` `` marker records the retired decision for readers and deduplicates future index appends; it does not itself prevent a fresh clone from reusing the slug, so reviewers must treat the tracked retirement as authoritative until Forge exposes a durable slug tombstone.
- Leave `spec/decisions/` ready for one tracked ephemeral ADR at a time; applied ADRs are deleted by `/update-spec` after rationale moves into SPEC, the index, and the commit.
- Label `docs/adr/` in contributor guidance as historical implementation records rather than the active product-spec decision lifecycle.

### 3. Correct contributor authority and gate guidance

- Describe `spec/` as tracked durable product authority.
- Describe `plans/phases.yaml` as a tracked design/decomposition snapshot whose live status, assignment, sequencing, and blockers remain GitHub-authoritative.
- Document the snapshot policy: routine tracker-state reads query GitHub and use read-only/dry-run reconciliation. A non-dry `reconcile --pull` is run only when its resulting snapshot will be committed, including the one-time post-bootstrap refresh required to replace the currently dangling `spec_revision`. This keeps `plans/phases.yaml` clean for `/update-spec`'s clean-target gate.
- Attribute npm exclusion to the exact `package.json#files` allowlist, including `data/`.
- Pin the full issue gate everywhere this repository documents the phase gate:
  `pnpm typecheck && pnpm build && pnpm test && pnpm test:scaffold-scripts && pnpm smoke`.
- Do not edit Forge-managed marker claims; tracking makes those generated statements true.

### 4. Verify live acceptance without weakening it

- Preserve issue #343's acceptance wording; do not narrow the criterion being graded.
- Audit the epic and every open child body. Repair any drifted open child body in-ticket while preserving Forge footers and dependencies.
- Confirm Roster-owned approval authority remains explicit in the non-goal list.
- Do not change the product architecture or introduce any runtime surface.

## Data and authority flow

```text
spec/BRIEF.md + spec/PRD.md + spec/SPEC.md
  -> spec/CONTEXT.md
  -> plans/phases.yaml (tracked design snapshot)
  -> GitHub issues (live execution state)

accepted ephemeral decision
  -> SPEC/PRD + affected task acceptance
  -> decision index + propagation commit
  -> ephemeral ADR deleted
```

## Edge cases

- The main checkout has a pre-existing user-owned `.forge/settings.yaml` edit. Never stage, overwrite, or copy it into this branch.
- Forge hydration copies root `spec/*.md` and `plans/**` but not `spec/decisions/**`; create the normalized decision index explicitly in this worktree.
- Removing the ignore rules exposes any stale hydrated files. Audit `git status --untracked-files=all` and stage an explicit allowlist only.
- `plans/phases.yaml` currently contains a `spec_revision` that cannot resolve because spec files were never tracked. After the first authority commit, run a non-dry reconcile and commit the refreshed snapshot so `spec-diff` has a real baseline.
- `plans/phases.yaml` tracker timestamps are not evidence of live task status; tracker queries remain mandatory. Never leave a non-dry reconciliation uncommitted when phases is an `/update-spec` target.
- Tracking spec/plans must not alter npm contents. Verify the dry-run tarball file list rather than assuming the allowlist works.
- Secret scanners do not detect business-identity disclosure; the human explicitly approved publishing the named adopter scenarios.

## Verification

1. `git check-ignore` proves canonical spec/plans are no longer ignored while `.forge` runtime files remain ignored.
2. Fresh-tree audit proves only the explicit canonical artifact set is untracked/tracked.
3. Forge spec ingestion/decomposition checks validate the tracked artifacts.
4. `gitleaks` reports no findings on the complete branch diff.
5. `npm pack --dry-run --json` proves `spec/` and `plans/` are absent from the package.
6. Full phase gate passes:
   - `pnpm typecheck`
   - `pnpm build`
   - `pnpm test`
   - `pnpm test:scaffold-scripts`
   - `pnpm smoke`
   Run all five locally. If either shell integration suite fails only because of a demonstrated macOS/Ubuntu portability difference, report the local failure honestly and require the corresponding Ubuntu CI check to pass; never label the local command passed.
7. Acceptance audit checks all four issue criteria against tracked files and live issue bodies.
8. Independent code/documentation review reports no blocking findings.
9. Latest Claude Opus implementation review scores security, code quality, and bugs at least 8/10 without changing the approved feature boundary.

## Shipping

- Force-add only `plans/tasks/343.plan.md` and commit the approved plan before implementation; it is still covered by the pre-bootstrap `/plans/` ignore rule.
- Stage every implementation artifact by explicit path, never `git add -A` or a directory-wide add. This excludes the user-owned `.forge/settings.yaml` change and any local legacy material.
- Commit the lifecycle bootstrap as a separate conventional commit.
- Run `forge orchestrate reconcile --pull` after that commit to replace the dangling `spec_revision`, inspect the exact phases diff, and commit the refreshed roadmap snapshot separately.
- Push the task branch and open a PR into `main` closing #343. Merge with a non-squash merge commit so the reconciled `spec_revision` authority commit remains reachable from `main`; do not squash or rebase-merge this bootstrap PR.
- Check CI once per goal turn. Merge only when every required check is green.
- After merge, preserve both main-checkout legacy files under `.forge/drafts/legacy/`. Verify every remaining untracked canonical `spec/` and `plans/` file is byte-identical to the incoming PR blob, then move those pre-bootstrap copies into `.forge/drafts/pre-343-main-snapshot/` rather than deleting them. Do not pull or clean the main checkout while its user-owned `.forge/settings.yaml` edit remains dirty; continue from `origin/main`-based task worktrees and report the deferred local fast-forward.
- Mark/confirm #343 complete, run a read-only reconcile check, and continue with #342.
