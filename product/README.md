# Global product agents

Logic for product agents. No agents yet — add via:

```bash
bash scripts/new-agent.sh product <agent-name>
```

When you add the first agent here, see `gtm/sdr/` as the canonical example.
