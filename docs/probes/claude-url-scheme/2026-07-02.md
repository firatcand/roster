# claude:// URL scheme probe — 2026-07-02

**Probe:** `scripts/probe-claude-url-scheme.sh` ([ROS-57](https://linear.app/firatdogan/issue/ROS-57))
**Claude Desktop version:** 1.15962.1 (build 1.15962.1)
**Host:** Darwin 25.4.0
**Result:** No schedule-creation routes (non-schedule literals recorded)

---

## 1. `Info.plist` — `CFBundleURLSchemes`

```
Array {
    Dict {
        CFBundleURLName = Claude
        CFBundleURLSchemes = Array {
            claude
        }
    }
}
```

## 2. asar literals — `grep -E 'claude://[a-zA-Z0-9_/.-]+'`

```
claude://claude.ai/mcp-auth-callback/sdk
claude://cowork/shared-artifact
claude://resume
```

## 3. Behaviour probes — `open -g`

- `claude://schedule` — attempted (open exited 0; no observable response means unhandled)
- `claude://schedule/new` — attempted (open exited 0; no observable response means unhandled)
- `claude://routine` — attempted (open exited 0; no observable response means unhandled)
- `claude://routine/new` — attempted (open exited 0; no observable response means unhandled)
- `claude://task/new` — attempted (open exited 0; no observable response means unhandled)
- `claude://tasks` — attempted (open exited 0; no observable response means unhandled)

---

## Interpretation

No schedule-creation route found. Two non-schedule literals are NEW since the 2026-05-18 probe — `claude://claude.ai/mcp-auth-callback/sdk` (MCP auth callback) and `claude://resume` — recorded without follow-up; the promotion signal is any `schedule|routine|task` route, and none exists. Confirms Spike β finding — UI hand-off remains the only viable Claude schedule install path until [anthropics/claude-code#41364](https://github.com/anthropics/claude-code/issues/41364) ships.

Next probe due: first Monday of next month.
