# Global design agents

Logic for design agents. No agents yet — add via:

```bash
bash scripts/new-agent.sh design <agent-name>
```

When you add the first agent here, see `gtm/sdr/` as the canonical example.
