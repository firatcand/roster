# direnv config — auto-loads forge's secrets when you cd into this repo.
# The secret values live in .forge/.env (gitignored); this file only points at it.
# `_if_exists` so it no-ops for collaborators who haven't created .forge/.env yet.
# After cloning, run `direnv allow` once to authorize.
dotenv_if_exists .forge/.env
